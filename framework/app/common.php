<?php

/**
 * OYTAPHP 公共函数文件
 * 
 * 本文件定义了全局可用的助手函数
 * 这些函数可以在项目的任何地方直接调用
 * 
 * 注意：以下助手函数由 Rust 运行时提供，这里仅作为文档参考：
 * - env()      获取环境变量
 * - config()   获取配置值
 * - cache()    缓存操作
 * - session()  Session操作
 * - view()     视图渲染
 * - json()     JSON响应
 * 
 * 以下路径助手函数由 Rust 运行时提供：
 * - app_path()     获取应用目录路径
 * - config_path()  获取配置目录路径
 * - public_path()  获取公共目录路径
 * - runtime_path() 获取运行时目录路径
 * - root_path()    获取项目根目录路径
 * - vendor_path()  获取vendor目录路径
 */

// 用户可以在这里定义自定义助手函数
